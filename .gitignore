# ── Environment variables — NEVER commit these ──
.env
.env.local
.env.development
.env.production

# ── Dependencies — too large for GitHub ──
node_modules/

# ── Build output ──
dist/
build/

# ── OS files ──
.DS_Store
Thumbs.db

# ── Editor files ──
.vscode/settings.json